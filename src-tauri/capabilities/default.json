{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window and tray popover: core APIs only. The frontend never talks to the database directly.",
  "windows": ["main", "popover"],
  "permissions": ["core:default"]
}
