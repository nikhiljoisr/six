{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs only. The frontend never talks to the database directly.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
